/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "www.freepik.com",
      "img.freepik.com",
      "i.ibb.co",
    ],
  },
};

export default nextConfig;
